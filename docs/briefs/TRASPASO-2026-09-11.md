# TRASPASO — 11 de septiembre de 2026

Para la sesión que siga desde otra cuenta, **sin memoria de lo anterior**. Léelo entero antes de
tocar nada: aquí está lo que está a medias, lo que NO se puede tocar y lo que ya se probó y falló.

---

## 0. LO PRIMERO: cómo se trabaja aquí

Estas reglas son suyas, ganadas a base de disgustos. No son estilo, son condiciones.

- **Nada que él note llega a `main` sin su OK.** `beta` sí se publica sin preguntar cuando está
  terminada y verificada, y se le avisa. `main` es suyo.
- **Todo lo mío lo revisa Cursor, y revisar = EJECUTAR los tests**, no leer el diff. Se coordina por
  el buzón `.claude/canal-equipo/messages/` (gitignored). Escribe en `messages/claude/`, lee
  `messages/cursor/`.
- **Una tanda nace en `tanda/<id>` desde `main`** para que pueda subir sola. Si se mezclan, hay que
  portarlas a mano; el cherry-pick con `-X theirs` ya compiló código duplicado una vez.
- **De uno en uno.** Nada se da por arreglado sin ejecutarlo contra sus datos reales.
- **Si él lo ve y tu medida sale limpia, tu medida está mal.** Ha pasado hoy tres veces.
- **Menos texto, más trabajo.** Resumen solo si lo pide.
- **Nunca editar ficheros con PowerShell** (`Get-Content|Set-Content` corrompe UTF-8).
- La versión canónica es el fichero `VERSION`, no `package.json`.

### Cosas que parecen bien y no lo están (todas me han mordido HOY)

- **Pasar la suite ANTES de commitear no vale.** `docs-frescura` compara contra el último commit que
  sube `VERSION`: hay que pasarla **en la punta**, con el commit hecho.
- **Nunca silenciar `git merge`.** Le puse `2>/dev/null`, la rama no existía, el merge falló mudo y
  estuve a un paso de publicar una integración VACÍA con un CHANGELOG que decía que llevaba los
  arreglos. Tras cada merge: ¿avanzó HEAD? ¿está el contenido en el árbol?
- **Los tests pasan igual de bien sobre el código viejo.** Verde no es prueba de que algo entrara.
- **Backticks en `bash -c` con comillas dobles se ejecutan.** Me ha destrozado mensajes de commit y
  entradas de CHANGELOG tres veces hoy. Usa la herramienta de escritura de ficheros o heredoc con
  comillas simples.
- **CRLF**: los `String.replace` multilínea fallan. Edita por líneas.

---

## 1. Dónde está cada cosa AHORA

| | versión | estado |
|---|---|---|
| **producción** (`main`) | **4.18.22** | publicada 11/9 17:15 · Tests y Pages verdes · verificado en vivo |
| **beta** | **4.19.44** (publicada `.1`) | la que él prueba |
| **APK producción** | **4.18.22 (código 45)** | ya ofrecida: `apk.json` live lo anuncia |
| **APK beta** | 4.19.40 (código 44) | la que tiene en su móvil |

Verifica siempre con `npm run salud` — **no te fíes de lo que diga un documento, ni de este**.

---

## 2. LO QUE ESTÁ A MEDIAS, por prioridad

### 2.1 ✅ Aely en producción · 4.18.22 · APK código 45 — HECHO (11/9, 17:15)

**Mergeada a `main` en `3bd855bd`.** Review de Cursor verde, y la línea roja comprobada OTRA VEZ
antes de mergear: `git diff main...tanda -- android/ src/ public/ scripts/` da **0 coincidencias** de
`applicationId|package_name|custom_url_scheme|keyAlias|storeFile`. En el árbol tras el merge:
`applicationId "com.micartera.app"` intacto, `app_name`=Aely, gradle 45/4.18.22. CI Tests y Pages
en verde, y verificado EN VIVO: Pages sirve `4.18.22` y `apk.json` anuncia el código 45.

Lo de abajo es el contexto de por qué, que sigue valiendo.

Lleva el rebrand a Aely al lado NATIVO de producción: nombre, icono y los literales de las
notificaciones. **Hoy, para su padre y su pareja, la app se sigue llamando «Mi Cartera».**

Él lo autorizó con condición literal: *«siempre y cuando no reviente nada ni pise nada»*. Y ya les
ha avisado del cambio de nombre.

**⚠⚠ LA LÍNEA ROJA:** `applicationId`, `package_name`, `custom_url_scheme` y la firma **NO SE TOCAN**.
Si cambia cualquiera, Android lo trata como una app NUEVA: su padre se queda con la vieja instalada
y **sus datos dentro**, y la nueva le llega vacía. Comprobado que el diff no los toca (0 coincidencias
por grep); **repite la comprobación tú antes de mergear**.

El `versionCode` es **45** y no es arbitrario: producción anuncia 42 pero su móvil lleva 44, y Android
no ofrece una actualización con código ≤ al instalado.

El asset del APK **ya está subido**, pero no se lo ofrece a nadie hasta que `apk.json` esté en `main`.

### 2.2 ⏳ Logos de banco — YA MEDIDOS de su launcher, faltan dos por afinar

**Al día 11/9 17:35:** Cursor los rehízo contra una captura del launcher de su móvil (los PNG de
Enable Banking eran el lockup, no el icono, y no valían). Punta: `tanda/logos-marcas-reales` @
`9094e681` · 4.19.47, con preview cara a cara en `docs/design/bancos/preview-launcher.html`.
Mirados en navegador: **Sabadell, MyInvestor y CaixaBank pasan.** Faltan dos:
· **Trade Republic** — su icono son DOS trazos cortos y gordos; el SVG pinta TRES, largos y finos
  (lee como el «≈» de matemáticas).
· **Revolut** — la R tiene un hueco entre el asta y el hombro: a tamaño pequeño lee «IR».
No integrar a beta hasta que esos dos pasen.

Lo de abajo es la historia de por qué, que sigue valiendo.

Pidió los logos reales de sus bancos. Se dibujaron **dos veces de memoria** y las dos salieron mal:
la primera eran las iniciales (`S`, `R`, `T`, `M`), la segunda unas formas que tampoco se reconocían.
Su reacción: *«qué puta mierda es esa, no son los logos»*.

**Es el mismo error que con el logo de Aely**, rechazado dos veces por lo mismo, y que solo salió bien
cuando se **midió el PNG original**.

**Ya están los originales en `docs/design/bancos/`**, sacados del **directorio de Enable Banking**
(campo `logo` de `bank-aspsps`) — la misma fuente por la que la app conecta con sus bancos. Hay LEEME
con qué parte de cada logo usar.

- Revolut y CaixaBank tienen trazo **oficial** en simple-icons (`revolut`, `caixabank`).
- **El gzip va al 99 % (326/330).** Si no caben, avisar; NO apretar el presupuesto.
- **Prueba de fuego:** tapa el nombre del banco en Cuentas. Si no sabes cuál es, no está hecho.
  Ningún test caza esto: hay que MIRARLO en el navegador.

### 2.3 🔴 EL 475 € DEL WIDGET — SIN EXPLICACIÓN, y hoy hay una oportunidad

Su queja más vieja viva: paga algo y **el widget suma mal**; al abrir la app, se corrige solo.

**Lo que ya se descartó hoy, para que no lo repitas:**
- No es la ventana de mes: cliente y servidor coinciden.
- No es el posible repetido: se arregló (4.18.19) pero con el parseo viejo **los dos lados ya
  coincidían**, así que no era la causa.
- App y servidor dan el mismo número sobre el mismo escenario (`presupuesto-servidor`, verde).

**❌ LA PISTA DE LAS 14:05 ERA FALSA. No empieces por ella.** (corregido 11/9, 17:30)

Decía que faltaban **81,06 €** en la nube. Salía de una etiqueta mentirosa de
`scripts/diag-widget.mjs`: la línea «SERVIDOR (lo que va al widget)» pasaba las filas **CRUDAS** a
`statsDelMes`, cuando `ingest` usa `filasComoLaApp` desde la 4.18.2. O sea que simulaba el ingest
**VIEJO** y lo etiquetaba como el de hoy. Con sus datos, esa etiqueta se equivoca en **332,10 €**.
Arreglado en `tanda/diag-widget-honesto` (pinta las dos cifras, cada una con su nombre).

**🔥 LA CAUSA DE VERDAD, medida (11/9, 17:00) — EMPIEZA POR AQUÍ:**

El índice único de `expenses` es `(user_id, fecha, importe, comercio)` y **`importe` lleva el
signo**. La clave de fusión del cliente (`keyOf`, `día|importe|comercio`) también. Así que cuando
Open Banking devuelve **el mismo movimiento con el signo cambiado**, ni la base de datos ni la app
lo ven repetido: entra como fila nueva y el mes se descuadra por el **doble** del importe.

Caso literal — misma `fecha`, mismo `comercio`, misma `nota`, mismo `ob_name`, mismo `source`:

| fila | importe | cat | creado |
|---|---|---|---|
| `53fe8a61` | **+247,26** | `traspaso` | 06/09 |
| `7fa6b8c3` | **−247,26** | `ingreso` | 10/09 |

Barrido desde el 1/6: **3 pares, ≈1.097,72 € de ruido, y SOLO en su usuario** — los otros dos
tienen 0 y 0, que son justo los que no tienen Open Banking. **Sus 248 lápidas de `state.deleted`
son él borrando esto a mano desde julio.**

Y **nada lo tapa**: `reconcileObDupes`, pese al nombre, devuelve **siempre `borrar:[]`**. Solo
recategoriza gemelos de cashback a «inversión».

**Con esto, la pantalla ACIERTA:** el servidor de hoy da gastado 505,44 → `1000 − 505,44 = 494,56`,
que es prácticamente el **491** de su captura. El widget no miente; la tabla tiene basura.

**⚠ Estamos ciegos en el banco que falla.** `logObAmbiguous` solo registra movimientos **sin
concepto y sin nombre** (más un `siempreTR` para Trade Republic). Resultado: **55 eventos OB desde
el 3/8, los 55 de TR, cero de Revolut y cero de CaixaBank** — y el duplicado de 247,26 € es de
Revolut. De TR sí se sabe: `entry_reference`, `transaction_id`, `reference_number` y
`bank_transaction_code` salen **null en 8 de 8**; lo único poblado es `credit_debit_indicator`,
`status`, `booking_date`, importe y **`balance_after_transaction`** — que nadie había mirado.

**Decisión pendiente (voto con Cursor):** índice sobre `abs(importe)` (barato pero rompe el caso
real de un +50 y un −50 el mismo día), actualizar en vez de insertar cuando coincida
`(fecha, comercio, abs(importe))`, o usar la clave del banco si Revolut/Caixa la mandan.
**Limpiar esas filas de su nube necesita su OK: no lleva marcha atrás.**

Hay una **foto del antes** en `%TEMP%/foto-ANTES-compra-11sep-1645.json` (11/9 16:45): 60 filas en
la tabla, 248 lápidas, presupuesto 1000, modo `net`, `app_state` tocado por última vez a las 12:05Z.

Se le pidió que al ir a comprar mandara cuatro fotos: widget antes, widget después **sin abrir la
app**, y app + widget después de abrirla. **Si las manda, son la evidencia que falta.**

**Propuesta pendiente de su OK:** que el widget deje rastro de qué cifras usó y cuándo, igual que se
hizo con el saldo del padre. Hoy, cuando se descuadra, no hay nada que mirar.

### 2.4 🟡 El saldo de Revolut de su PADRE

Se queja de que cambia solo sin tocar la cuenta. **Causa encontrada:** esa cuenta tiene
`role: "ambos"`, así que el número **no es el del banco**: es derivado, y se recalcula con los
movimientos del mes. Por eso se mueve solo.

Ya está desplegado el **rastro** (`balTipo`, `balTipos`, `balSaldo`, versión 4.18.20): en su próxima
sincronización sabremos qué saldo manda el banco de verdad.

**⚠ Por qué todavía no hay rastro que mirar (medido 11/9, 17:20): su padre corre la 4.18.18.**
`app_events` dice la última versión vista de cada uno: padre **4.18.18** (hoy 09:52Z), pareja
4.18.21, él 4.19.44.1. El rastro entró en la **4.18.20**, así que su padre aún no ha ejecutado ese
código. No es un fallo: es que no ha abierto la app desde entonces. Con la 4.18.22 ya publicada,
en cuanto la abra empezará a escribirlo. **No re-diagnostiques esto hasta ver `balTipo` en su
`app_state`.** (Y `obAccounts: []` en su estado **es normal**, no un enlace roto: al adoptar una
cuenta del banco en `accounts`, `adoptObAccount` la QUITA de `obAccounts`. Sus tres cuentas están
adoptadas.)

**Pendiente:** mirarlo cuando sincronice,
y preguntarle si su padre gasta con esa cuenta (si no, el rol correcto es «recibos» y deja de bailar).

---

## 3. Decisiones suyas de hoy — NO las reabras

- **«Ver más» en Recibos se queda.** Recibos enseña 3 pendientes + 3 pagados y no scrollea nunca.
  Medido: Deudas 1186/808 y Metas 1331/808 **sí** scrollean. Él: *«me vale el ver más, no toques eso»*.
- **Brokers (puerta única): NO se hace.** Se midió su patrimonio por entidad y **no hay doble conteo**
  (sus cuentas de TR y MyInvestor se llaman «Efectivo» y llevan solo el cash). Él confirmó que «lo del
  descuajaringa ya está arreglado». Reconstruir Cartera sería arreglar algo que no está roto.
- **El rebote «se baja todo en bloque»: NO reproducido.** Llegué a poner `overscroll-behavior-y:
  contain` y **lo retiré**: el shell no se mueve ni un píxel (215 fotogramas) y dos capturas con el
  dedo puesto salen con el mismo sha1. Hace falta verlo pasar antes de tocar el scroll de la familia.
- **Que en la cartera vacía no se oculte la barra ni salga la ola NO es un bug**: sin nada que
  scrollear no hay eventos de scroll.
- **Ajustes → Dinero ya está limpio** desde la 4.14.0. Y **`Moneda de visualización` SÍ funciona** hoy
  (la leen tres sitios): el inventario de agosto dice que no, y está rancio. **Si la quitas, le rompes
  la conversión.**

---

## 4. ⚠ Seguridad — pendiente que haga ÉL

El 11/9 imprimí `android/local.properties` entero en la conversación mientras depuraba. Ese fichero
lleva **la contraseña del keystore de firma** y **el token de ingest**. Está gitignored (no salió al
repo público) pero quedó escrito en un historial de chat.

**Se le pidió rotar el token de ingest (lo urgente) y la contraseña del keystore (menos grave: sin el
fichero `.keystore`, que nunca salió de su máquina, no sirve).** Comprueba si lo ha hecho.

**No vuelvas a volcar ese fichero.** Para ver una clave suelta, `grep` de esa clave.

---

## 5. Cosas del entorno que te van a morder

- **Su móvil es un OnePlus 13** (Android 16, WebView Chromium 150). El repo lo dice mal en ~35 sitios.
- **La APK pública no lleva depuración.** Para medir en su móvil hay una **`.debug` instalada en
  paralelo** (`com.micartera.app.debug`), que NO toca sus datos. Se le engancha por
  `adb forward` + CDP sobre `webview_devtools_remote_<pid>`.
- **Para medir gestos, usa `adb shell input swipe` con duración larga**, no toques sintéticos del
  navegador: el muestreo del digitalizador es donde vivía el tironcillo.
- **El service worker sirve la copia vieja** y te hace revisar un bundle que no es el del disco. Si
  lo que ves no cuadra con el fichero, desregistra el SW y borra cachés antes de acusar a nadie.
- **`git push origin beta` falla**: hay un tag `beta` además de la rama. Usa
  `git push origin refs/heads/beta:refs/heads/beta`.
- **Gradle no traga rutas de worktree** (`.claude/worktrees/...`): compila desde el checkout principal.
- Hay un **token de solo lectura** y una **service key** en `.env.local` (gitignored). Él autorizó
  mirar su estado en la nube **y el de su padre**. Los otros usuarios, no.

---

## 6. Ramas vivas sin integrar

| rama | versión | qué es |
|---|---|---|
| `tanda/aely-a-produccion` | 4.18.22 | §2.1, esperando review |
| `tanda/logos-marcas-reales` | 4.19.45 | logos dibujados a ojo — **NO integrar**, hay que rehacer midiendo |
| `tanda/logos-letra-11sep` | 4.19.44 | idem, aparcada |
| `tanda/conectar-cuentas-11sep` | 4.19.45 | ya integrado su contenido por otra vía |
| `tanda/fin07-paginado`, `tanda/fin-07-pull` | 4.19.36 | **MUERTAS**, quedó `fin07-historico` |
| `prod/panel-ronda`, `prod/notas-20`, `prod/revision-plegable` | 4.18.14-16 | nacidas de `main` antes del recorte de notas: meten la nota en el módulo en vez de en `src/data/release-notes.json`. **NO mergear**; Cursor hizo las `-v2` |
| `tanda/tiron-raf` | 4.19.30 | rancia, su contenido ya está en beta |

`arranque-suelto` (aprobada por él) **NO es portable a producción**: depende de `inicioDeMesMs`, que
viene de la ventana de mes que **él rechazó**. Lo cazó Cursor.

---

## 7. Lo que se hizo hoy, en una línea cada uno

FIN-07 (producción borraba gastos al sincronizar) · el histórico entero paginado por clave · el modo
inicial que la nube volvía a llenar · dos logos distintos en la app · el icono que se cortaba en las
notificaciones · **el tironcillo del cambio de pestaña** (36,1 px de brinco → 0,7, medido en su móvil
con toques reales) · el rescate de las diez tandas aprobadas que llevaban desde el 9/9 sin llegar a
su familia · las notas de versión fuera del bundle (−38 KB de gzip) · el repetido que el servidor
contaba por accidente · el rastro del saldo del banco.

**Y lo que más se repitió como error: dar por buena una causa sin reproducirla.** Pasó tres veces en
un día. Si no lo has visto fallar, no lo has arreglado.

---

# ACTUALIZACION DE LA TARDE (11/9, 19:45) — lo que cambio despues de escribir esto

## Estado real, verificado EN VIVO (no leido de un documento)

| | version | comprobado |
|---|---|---|
| produccion web | **4.18.24** | `version.json` de Pages |
| APK produccion | **4.18.22 (codigo 45)** | `apk.json` de Pages · **es Aely** |
| beta | **4.19.50.1** | `version.json` del release `beta` |

`main` @ `2caf5626`, `beta` @ `722dda5a`, las dos con CI verde.

## EL 512/497: CAZADO EN VIVO, con sus tres fotos

Compro a las 12:28Z (6,49 EUR en Consum, por macrodroid) y mando las tres capturas:

1. widget **sin abrir la app**: `512 EUR · te quedan 488`
2. abre la app: Inicio dice **497**
3. widget despues: **497 · te quedan 503**

**El servidor reproduce el widget al centimo.** `filasComoLaApp` + `statsDelMes` sobre la tabla:
`gastado 511,93 · te quedan 488,07`. Eso es el 512/488 de su foto. **Ya no hay que adivinar lo que
ve el widget: se simula.**

Y la app cuenta **14,90 EUR mas de ingreso**:

```
widget : bruto 1028,71 - ingresos 516,78 = 511,93  -> 512
app    : bruto 1028,71 - ingresos 531,68 = 497,03  -> 497
```

`516,78 + 14,90 = 531,68`. Exacto. La tabla tiene 9 filas de +-14,90 este mes; tras lapidas y
fusion sobreviven **4** (dias 02, 03, 04 y 05, todas `Bizum recibido`). La app cuenta **5**.

Pista: en `state.deleted` hay dos lapidas con comercio **`Bizum`** a secas (`2026-09-06|-14.9|Bizum`
y `2026-09-03|-14.9|Bizum`) y **en la tabla no existe ninguna fila con ese comercio**. Su movil
tiene o tuvo Bizums que la nube nunca guardo.

**LO QUE FALTA, y solo puede hacerlo el:** abrir Gastos y decir **cuantos `Bizum recibido` de
14,90 EUR le salen este mes y de que dias**. Si hay uno del **06/09**, cerrado.

## El mecanismo, y ya esta arreglado en las dos ramas

Habia **ocho** escrituras de gastos a la nube con `.catch(function(){})` — mudas. Una fila que la
tabla rechaza se quedaba en el movil para siempre y nadie se enteraba. La peor:
`04-tab-gastos.js` al **editar** un gasto (borra y vuelve a insertar): si el borrado va bien y el
alta falla, **el gasto desaparece de la nube entero**.

Ahora hay `subirGasto(e, donde)` / `borrarGastoNube(e, donde)` en `00-core.js` que registran en
`app_events` la clave del gasto y el error. Produccion **4.18.24**, beta **4.19.50**.

> **Lo primero que hay que hacer manana: `node scripts/errores.mjs`.** En cuanto el sincronice,
> `app_events` deberia cantar QUE fila no sube y por que. Es la primera vez que vamos a tener el
> dato en vez de suponerlo.

## Lo que rompi y como

**Los logos dejaron la beta sin publicar.** `e2e/pulido-vacios.spec.mjs` usaba
`page.locator("svg circle").last()`. Los logos entran como SVG inline con sus propios `<circle>`,
asi que el ultimo circulo del documento paso a ser **el punto azul de Sabadell** (r=3,55, sin
`stroke-dasharray`) en vez del anillo (r=48). Reproducido: pasa en `f7cc8902`, falla en `1f7b4fc8`.
Arreglado acotando a `.v4-budget svg circle`. **Segunda vez que un locator sin acotar nos muerde.**

**Y un descubrimiento del entorno:** falta el shim de `node_modules/.bin`, por eso `npm run test:e2e`
decia «playwright no se reconoce». Esto SI funciona y ya esta cableado en el runner de las dos ramas:

```
node node_modules/@playwright/test/cli.js test --config=playwright.config.mjs
```

**Ojo con una trampa de lectura:** si `memoria-espejo` sale rojo, el runner **aborta antes de los
e2e** y la salida ni menciona playwright. Comprobar que el bloque `-- playwright-e2e --` EXISTE,
no solo que no hay «FAILED».

## Lo demas que se movio

- **Aely esta en produccion.** `3bd855bd`, 4.18.22, APK codigo 45. Comprobada la linea roja yo
  mismo antes de mergear: 0 coincidencias de `applicationId|package_name|custom_url_scheme` fuera
  de docs. Su padre y su pareja ven el nombre nuevo sin perder datos.
- **Logos:** los cinco pasan la prueba de fuego a 40 px. Revolut ya es una R de una pieza (antes
  leia «IR») y TR son dos trazos con aire (antes tres, leia «~»). En beta 4.19.49.
- **`diag-widget.mjs` mentia** y esta arreglado (`main`): etiquetaba «lo que va al widget» un
  calculo con filas crudas. Ahora pinta las dos cifras y caza solo los signos volteados.
- **Los signos volteados siguen vivos** como bug, pero **no son la causa del 512/497**: esas dos
  filas ya estaban tombstoneadas. Son la explicacion de sus 248 lapidas. Voto de Cursor: opcion 2
  (buscar `(fecha, comercio, abs(importe))` y actualizar en vez de insertar). **Limpiar sus filas
  necesita su OK.**
- **Seguimos ciegos en Revolut:** 55 eventos OB desde el 3/8, los 55 de Trade Republic. El filtro
  de `logObAmbiguous` solo guarda movimientos sin concepto NI nombre, y los de Revolut y Caixa
  siempre traen nombre. De TR: `entry_reference`/`transaction_id`/`reference_number` null en 8 de
  8; lo unico util es `balance_after_transaction`, que nadie habia mirado.

## Pendiente de EL

1. Los `Bizum recibido` de 14,90 en Gastos (arriba).
2. OK para limpiar los 3 pares de signo volteado de su nube.
3. OK para desplegar la muestra por banco de `logObAmbiguous` (Edge = produccion).
4. Rotar el token de ingest (sigue pendiente del incidente de la manana).
5. Su padre corre la **4.18.18**; el rastro del saldo entro en la 4.18.20. Por eso no hay `balTipo`
   que mirar. No es un fallo: no ha abierto la app. (`obAccounts: []` tambien es normal:
   `adoptObAccount` las quita de ahi al adoptarlas.)
