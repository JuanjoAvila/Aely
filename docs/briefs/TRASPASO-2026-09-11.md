# TRASPASO — 11 de septiembre de 2026

Para la sesión que siga desde otra cuenta, **sin memoria de lo anterior**. Léelo entero antes de
tocar nada: aquí está lo que está a medias, lo que NO se puede tocar y lo que ya se probó y falló.

---

## 0. LO PRIMERO: cómo se trabaja aquí

Estas reglas son suyas, ganadas a base de disgustos. No son estilo, son condiciones.

- **Nada que él note llega a `main` sin su OK.** `beta` sí se publica sin preguntar cuando está
  terminada y verificada, y se le avisa. `main` es suyo.
- **Todo lo mío lo revisa Cursor, y revisar = EJECUTAR los tests**, no leer el diff. Se coordina por
  el buzón `.claude/canal-equipo/messages/` (gitignored). Escribe en `messages/claude/`, lee
  `messages/cursor/`.
- **Una tanda nace en `tanda/<id>` desde `main`** para que pueda subir sola. Si se mezclan, hay que
  portarlas a mano; el cherry-pick con `-X theirs` ya compiló código duplicado una vez.
- **De uno en uno.** Nada se da por arreglado sin ejecutarlo contra sus datos reales.
- **Si él lo ve y tu medida sale limpia, tu medida está mal.** Ha pasado hoy tres veces.
- **Menos texto, más trabajo.** Resumen solo si lo pide.
- **Nunca editar ficheros con PowerShell** (`Get-Content|Set-Content` corrompe UTF-8).
- La versión canónica es el fichero `VERSION`, no `package.json`.

### Cosas que parecen bien y no lo están (todas me han mordido HOY)

- **Pasar la suite ANTES de commitear no vale.** `docs-frescura` compara contra el último commit que
  sube `VERSION`: hay que pasarla **en la punta**, con el commit hecho.
- **Nunca silenciar `git merge`.** Le puse `2>/dev/null`, la rama no existía, el merge falló mudo y
  estuve a un paso de publicar una integración VACÍA con un CHANGELOG que decía que llevaba los
  arreglos. Tras cada merge: ¿avanzó HEAD? ¿está el contenido en el árbol?
- **Los tests pasan igual de bien sobre el código viejo.** Verde no es prueba de que algo entrara.
- **Backticks en `bash -c` con comillas dobles se ejecutan.** Me ha destrozado mensajes de commit y
  entradas de CHANGELOG tres veces hoy. Usa la herramienta de escritura de ficheros o heredoc con
  comillas simples.
- **CRLF**: los `String.replace` multilínea fallan. Edita por líneas.

---

## 1. Dónde está cada cosa AHORA

| | versión | estado |
|---|---|---|
| **producción** (`main`) | **4.18.21** | publicada, tres CI verdes, Edge desplegado |
| **beta** | **4.19.44** (publicada `.1`) | la que él prueba |
| **APK producción** | 4.18.3 (código 42) | **desfasada**, ver §2 |
| **APK beta** | 4.19.40 (código 44) | la que tiene en su móvil |

Verifica siempre con `npm run salud` — **no te fíes de lo que diga un documento, ni de este**.

---

## 2. LO QUE ESTÁ A MEDIAS, por prioridad

### 2.1 ⏳ `tanda/aely-a-produccion` · 4.18.22 · APK código 45 — ESPERANDO REVIEW DE CURSOR

Lleva el rebrand a Aely al lado NATIVO de producción: nombre, icono y los literales de las
notificaciones. **Hoy, para su padre y su pareja, la app se sigue llamando «Mi Cartera».**

Él lo autorizó con condición literal: *«siempre y cuando no reviente nada ni pise nada»*. Y ya les
ha avisado del cambio de nombre.

**⚠⚠ LA LÍNEA ROJA:** `applicationId`, `package_name`, `custom_url_scheme` y la firma **NO SE TOCAN**.
Si cambia cualquiera, Android lo trata como una app NUEVA: su padre se queda con la vieja instalada
y **sus datos dentro**, y la nueva le llega vacía. Comprobado que el diff no los toca (0 coincidencias
por grep); **repite la comprobación tú antes de mergear**.

El `versionCode` es **45** y no es arbitrario: producción anuncia 42 pero su móvil lleva 44, y Android
no ofrece una actualización con código ≤ al instalado.

El asset del APK **ya está subido**, pero no se lo ofrece a nadie hasta que `apk.json` esté en `main`.

### 2.2 ⏳ Logos de banco — CONGELADOS, esperando que Cursor los MIDA

Pidió los logos reales de sus bancos. Se dibujaron **dos veces de memoria** y las dos salieron mal:
la primera eran las iniciales (`S`, `R`, `T`, `M`), la segunda unas formas que tampoco se reconocían.
Su reacción: *«qué puta mierda es esa, no son los logos»*.

**Es el mismo error que con el logo de Aely**, rechazado dos veces por lo mismo, y que solo salió bien
cuando se **midió el PNG original**.

**Ya están los originales en `docs/design/bancos/`**, sacados del **directorio de Enable Banking**
(campo `logo` de `bank-aspsps`) — la misma fuente por la que la app conecta con sus bancos. Hay LEEME
con qué parte de cada logo usar.

- Revolut y CaixaBank tienen trazo **oficial** en simple-icons (`revolut`, `caixabank`).
- **El gzip va al 99 % (326/330).** Si no caben, avisar; NO apretar el presupuesto.
- **Prueba de fuego:** tapa el nombre del banco en Cuentas. Si no sabes cuál es, no está hecho.
  Ningún test caza esto: hay que MIRARLO en el navegador.

### 2.3 🔴 EL 475 € DEL WIDGET — SIN EXPLICACIÓN, y hoy hay una oportunidad

Su queja más vieja viva: paga algo y **el widget suma mal**; al abrir la app, se corrige solo.

**Lo que ya se descartó hoy, para que no lo repitas:**
- No es la ventana de mes: cliente y servidor coinciden.
- No es el posible repetido: se arregló (4.18.19) pero con el parseo viejo **los dos lados ya
  coincidían**, así que no era la causa.
- App y servidor dan el mismo número sobre el mismo escenario (`presupuesto-servidor`, verde).

**🔥 PISTA NUEVA Y MEDIDA (11/9, 14:05) — EMPIEZA POR AQUÍ:**

Mandó dos capturas a la vez: **el widget dice 491 € y la app dice 491 €**. O sea que en reposo
**coinciden**; el widget no miente por su cuenta.

Pero calculando desde la NUBE con sus 70 gastos del mes sale **409,94** (`against`, modo `net`:
spent 1308,82 − income 898,88, budget 1000, reserved 0). Su pantalla dice 491.

> **Hay 81,06 € que su móvil cuenta y la tabla `expenses` de la nube no tiene.**

Y esa es justo la forma del bug que describe: **el widget se alimenta del servidor (`ingest`) y la
app de su estado local merged**. Cuando esos dos divergen, el widget dice una cosa, la app otra, y al
abrir la app manda ella y «lo corrige» — que es literalmente su queja desde el principio.

**Lo siguiente que hay que hacer:** averiguar QUÉ son esos 81,06 €. Candidatos: gastos locales que
nunca subieron a la tabla, o gastos de la tabla que el cliente excluye y el servidor no (o al revés).
`slimForCloud` NO sube `expenses` dentro de `app_state` (van en su tabla), así que la tabla debería
ser la verdad — y no lo es.

Se le pidió que al comprar mandara: widget **antes de abrir la app**, y widget + Inicio **después**.
Si el widget salta a un número raro antes de que la app toque nada, el fallo entra por el servidor.

Hay una **foto del antes** en `%TEMP%/foto-antes-compra.json` (11/9 ~14:05): 70 gastos en la tabla,
against 173,34, spent 1022,22, income 848,88, modo `net`.

Se le pidió que al ir a comprar mandara cuatro fotos: widget antes, widget después **sin abrir la
app**, y app + widget después de abrirla. **Si las manda, son la evidencia que falta.**

**Propuesta pendiente de su OK:** que el widget deje rastro de qué cifras usó y cuándo, igual que se
hizo con el saldo del padre. Hoy, cuando se descuadra, no hay nada que mirar.

### 2.4 🟡 El saldo de Revolut de su PADRE

Se queja de que cambia solo sin tocar la cuenta. **Causa encontrada:** esa cuenta tiene
`role: "ambos"`, así que el número **no es el del banco**: es derivado, y se recalcula con los
movimientos del mes. Por eso se mueve solo.

Ya está desplegado el **rastro** (`balTipo`, `balTipos`, `balSaldo`, versión 4.18.20): en su próxima
sincronización sabremos qué saldo manda el banco de verdad. **Pendiente:** mirarlo cuando sincronice,
y preguntarle si su padre gasta con esa cuenta (si no, el rol correcto es «recibos» y deja de bailar).

---

## 3. Decisiones suyas de hoy — NO las reabras

- **«Ver más» en Recibos se queda.** Recibos enseña 3 pendientes + 3 pagados y no scrollea nunca.
  Medido: Deudas 1186/808 y Metas 1331/808 **sí** scrollean. Él: *«me vale el ver más, no toques eso»*.
- **Brokers (puerta única): NO se hace.** Se midió su patrimonio por entidad y **no hay doble conteo**
  (sus cuentas de TR y MyInvestor se llaman «Efectivo» y llevan solo el cash). Él confirmó que «lo del
  descuajaringa ya está arreglado». Reconstruir Cartera sería arreglar algo que no está roto.
- **El rebote «se baja todo en bloque»: NO reproducido.** Llegué a poner `overscroll-behavior-y:
  contain` y **lo retiré**: el shell no se mueve ni un píxel (215 fotogramas) y dos capturas con el
  dedo puesto salen con el mismo sha1. Hace falta verlo pasar antes de tocar el scroll de la familia.
- **Que en la cartera vacía no se oculte la barra ni salga la ola NO es un bug**: sin nada que
  scrollear no hay eventos de scroll.
- **Ajustes → Dinero ya está limpio** desde la 4.14.0. Y **`Moneda de visualización` SÍ funciona** hoy
  (la leen tres sitios): el inventario de agosto dice que no, y está rancio. **Si la quitas, le rompes
  la conversión.**

---

## 4. ⚠ Seguridad — pendiente que haga ÉL

El 11/9 imprimí `android/local.properties` entero en la conversación mientras depuraba. Ese fichero
lleva **la contraseña del keystore de firma** y **el token de ingest**. Está gitignored (no salió al
repo público) pero quedó escrito en un historial de chat.

**Se le pidió rotar el token de ingest (lo urgente) y la contraseña del keystore (menos grave: sin el
fichero `.keystore`, que nunca salió de su máquina, no sirve).** Comprueba si lo ha hecho.

**No vuelvas a volcar ese fichero.** Para ver una clave suelta, `grep` de esa clave.

---

## 5. Cosas del entorno que te van a morder

- **Su móvil es un OnePlus 13** (Android 16, WebView Chromium 150). El repo lo dice mal en ~35 sitios.
- **La APK pública no lleva depuración.** Para medir en su móvil hay una **`.debug` instalada en
  paralelo** (`com.micartera.app.debug`), que NO toca sus datos. Se le engancha por
  `adb forward` + CDP sobre `webview_devtools_remote_<pid>`.
- **Para medir gestos, usa `adb shell input swipe` con duración larga**, no toques sintéticos del
  navegador: el muestreo del digitalizador es donde vivía el tironcillo.
- **El service worker sirve la copia vieja** y te hace revisar un bundle que no es el del disco. Si
  lo que ves no cuadra con el fichero, desregistra el SW y borra cachés antes de acusar a nadie.
- **`git push origin beta` falla**: hay un tag `beta` además de la rama. Usa
  `git push origin refs/heads/beta:refs/heads/beta`.
- **Gradle no traga rutas de worktree** (`.claude/worktrees/...`): compila desde el checkout principal.
- Hay un **token de solo lectura** y una **service key** en `.env.local` (gitignored). Él autorizó
  mirar su estado en la nube **y el de su padre**. Los otros usuarios, no.

---

## 6. Ramas vivas sin integrar

| rama | versión | qué es |
|---|---|---|
| `tanda/aely-a-produccion` | 4.18.22 | §2.1, esperando review |
| `tanda/logos-marcas-reales` | 4.19.45 | logos dibujados a ojo — **NO integrar**, hay que rehacer midiendo |
| `tanda/logos-letra-11sep` | 4.19.44 | idem, aparcada |
| `tanda/conectar-cuentas-11sep` | 4.19.45 | ya integrado su contenido por otra vía |
| `tanda/fin07-paginado`, `tanda/fin-07-pull` | 4.19.36 | **MUERTAS**, quedó `fin07-historico` |
| `prod/panel-ronda`, `prod/notas-20`, `prod/revision-plegable` | 4.18.14-16 | nacidas de `main` antes del recorte de notas: meten la nota en el módulo en vez de en `src/data/release-notes.json`. **NO mergear**; Cursor hizo las `-v2` |
| `tanda/tiron-raf` | 4.19.30 | rancia, su contenido ya está en beta |

`arranque-suelto` (aprobada por él) **NO es portable a producción**: depende de `inicioDeMesMs`, que
viene de la ventana de mes que **él rechazó**. Lo cazó Cursor.

---

## 7. Lo que se hizo hoy, en una línea cada uno

FIN-07 (producción borraba gastos al sincronizar) · el histórico entero paginado por clave · el modo
inicial que la nube volvía a llenar · dos logos distintos en la app · el icono que se cortaba en las
notificaciones · **el tironcillo del cambio de pestaña** (36,1 px de brinco → 0,7, medido en su móvil
con toques reales) · el rescate de las diez tandas aprobadas que llevaban desde el 9/9 sin llegar a
su familia · las notas de versión fuera del bundle (−38 KB de gzip) · el repetido que el servidor
contaba por accidente · el rastro del saldo del banco.

**Y lo que más se repitió como error: dar por buena una causa sin reproducirla.** Pasó tres veces en
un día. Si no lo has visto fallar, no lo has arreglado.
