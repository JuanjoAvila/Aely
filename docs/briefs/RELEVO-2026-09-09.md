# Relevo — madrugada del 9 de septiembre de 2026

**Lectura de entrada.** Sustituye a `RELEVO-CLAUDE-CURSOR-2026-09-08.md` como foto actual.
Claude dirige, integra y publica; Cursor implementa y **ejecuta los e2e**; Codex quedó libre tras
entregar B09-D y la arquitectura de identidad. Esta foto caduca: al retomar, `git fetch origin`,
`npm run salud`, `npm run listo` y mirar el último run.

## Dónde está todo

| | |
|---|---|
| Canal beta (lo que tiene su móvil) | **4.19.13.1** · commit `a2abcf61` · CI verde |
| Rama `beta` | `4b2ce834` (encima, solo documentación) |
| Producción (`main`, Pages) | **4.18.7** · sin tocar |
| APK | **42 / 4.18.3** · nada de esta ronda es nativo |
| Tandas pendientes de su veredicto | **15** |

`main` no tiene ni un commit de código que `beta` no tenga: comprobado, así que un promote de la
ronda entera no se traga nada por el `-X theirs` ([[promote-merge-theirs]]).

## Lo que entró esta noche, de 4.19.6 a 4.19.13

- **B09-D**, la causa del descuadre widget/Inicio: un posible repetido solo existía en el móvil.
  La app lo excluía y el servidor lo sumaba (app 40 / servidor 70, reproducido). La marca viaja
  ahora dentro de `source` como `ob:ent#dup`, **con sufijo y no prefijo nuevo a propósito**: un
  servidor sin desplegar lee un banco desconocido y lo EXCLUYE, en vez de leer «sin banco» y
  sumarlo. Por eso el arreglo no depende de desplegar el Supabase compartido.
- **Import histórico seguro, entero** (tandas 1 a 4): los cuatro agujeros A/B/C/N cerrados, más
  la tabla de «importantes». Es lo que él había marcado como lo que más le importa.
- **Efectivo** como una cuenta más, **informe del mes cerrado**, **límite por categoría**.
- **El panel de beta ya no le resucita las tandas aprobadas** — bug que metí yo en la 4.19.6.

## Lo que se paró antes de que le llegara

Seis, y ninguno se habría visto leyendo el diff: hicieron falta ejecuciones y simulaciones.

1. Los `cloudIds` del ACK venían crudos del servidor: un id ajeno podía llegar al borrado del undo.
2. El borrado en la nube iba a fuego y olvido y pintaba ✓ sin haber borrado nada.
3. El aviso de fallo decía «vuelve a deshacer» con el botón ya desaparecido.
4. `histCanUndo` (de Cursor) dejaba invisible el botón justo en el estado que crea el reintento (mío).
5. La detección de cajero entró en `autoCategory`, y una migración que corre en cada carga le
   habría convertido las retiradas viejas en `traspaso` —neutra— bajándole meses ya cerrados.
6. El desglose por categoría subió la cabecera y rompió el arrastrar-para-ordenar, que es una
   tanda que él **todavía no ha probado**: la habría culpado a la tanda vieja.

## Guardianes nuevos, y los bugs que encontraron solos

- `beta-tandas-vacias` — `tandas:[]` y «sin `tandas`» dejan de ser lo mismo.
- `novedades-idiomas` — encontró que la **4.18.5 le enseñaba castellano** a quien tuviera la app
  en inglés o catalán. Era una sugerencia suya del 26/7 sin atender.
- `categorias-dual` — saca las **688 claves** del fichero del servidor y exige la misma respuesta
  a los dos lados. Encontró que **«DOUGLAS» caía en otra categoría** según entrara por
  notificación o a mano.
- Espejos comparados de frente en `presupuesto-servidor` — encontró que una **fecha corrupta en
  `reservaLog`** se contaba en el cliente y no en el servidor: la app le enseñaría MENOS
  presupuesto que el widget.

Los tres últimos son la misma familia que el bug de los 965 €: una regla escrita dos veces y un
test que solo miraba una.

## Bloqueado esperando decisión suya

| Qué | Dónde |
|---|---|
| Divisa sin tipo de cambio contada 1 a 1 (500 ₺ → 500 €) | `divisa-desconocida-uno-a-uno.md`, tres caminos |
| Tanda «meta ahorrada = gasto del mes» — sus dos metas están a 0, no notaría nada | `plan-tanda-5-meta-gasto-mes.md` |
| Borrar ramas y worktrees viejos (13 worktrees, ~20 ramas) | la norma dice preguntar antes |
| Las dos sugerencias sin atender: el mensaje al actualizar cuentas (suya) y entrar a ajustes tocando la cartera (de su pareja, 16/7) | ambiguas: hay que preguntarle a QUÉ se refiere, no adivinar |
| Desplegar la Edge Function: la mitad del cajero y el `#dup` del servidor | toca producción |

## Normas nuevas de esta noche

- **Todo lo de Claude pasa por Cursor antes de publicar**, y revisar significa **ejecutar** los
  e2e, no leer el diff. La primera vez que se aplicó sin ejecutar, la CI se cayó y la beta **no se
  publicó**: se quedó con el bug en el móvil creyendo que estaba arreglado.
- **Cursor solo en worktrees propios.** Esta noche me encontré tres veces el checkout principal
  con la rama cambiada, una en medio de un cherry-pick ajeno.
- `git switch beta`, nunca `git checkout refs/heads/beta` (hay un tag `beta` y desengancha), y
  `git push origin refs/heads/beta:refs/heads/beta`.
