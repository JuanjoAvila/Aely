# FIN-05: relevo de la candidata 4.26.49

- Base: `main` en `95070a37`. Rama aislada: `codex/fin05-widget-arbitraje`.
- Código revisado: `e1168c6de3dcb5c39726eb3d619fb3b59cad3581`. Claude respondió PASS por el canal compartido tras tres rondas; la última fue sobre ese SHA. El árbol quedó limpio tras la revisión.
- Estado: código candidato, sin push a `beta` ni `main`; Edge no desplegada; APK no subida ni instalada. `public/apk.json` y `android/app/build.gradle` están alineados en 4.26.49/code 50 para preparar la entrega autorizada. El manifiesto de la rama no puede llegar a beta hasta verificar que el asset existe.
- Alcance: arbitraje nativo de app/ingest, orden inverso, journal de eventos no cubiertos, lápidas, nuevo mes, liquidez y efectivo separados. La Edge añade `eventKey`, `periodStart`, `readAt` y deltas exactos; una Edge anterior sigue funcionando y muestra «—» en el cruce cuya cifra no puede reconstruirse. La app espera el pull antes de reenviar su foto al widget y cubre tanto `ingest_event_id` como ID de fila.
- Evidencia local: `npm run build`; test de sintaxis; `widget-arbitraje`, `widget-coherente`, `ingest-handler` y presupuesto de bundle; compilación Java Release; 11 E2E dirigidos de persistencia/banco. Dos pasadas globales E2E previas: 430/431 (1 omitida) y 428/431 (2 fallos de pull solapado); corregidos esos dos casos, los 11 E2E afectados pasaron. `npm test` solo falla `memoria-espejo` por dos archivos de memoria externa a esta tanda; Deno no está instalado y su etapa se omite.
- `npm run release:apk -- --dry-run` generó y verificó localmente `android/app/build/outputs/apk/release/Aely-4.26.49.apk`, firmado `CN=Mi Cartera`, `applicationId=com.micartera.app`, code 50, sin WEB_DEBUG ni INGEST_URL en BuildConfig. SHA-256: `6B0A9956C907A3ED3F5E36813271452AD6A5CF88113F9DA425C70CEACAB9A66A`; tamaño 6.461.910 bytes. Para el dry-run se alineó temporalmente `build.gradle` a 4.26.49/code 50 y después se restauró junto con los archivos generados de Capacitor. Revalidar el artefacto si se recompila.
- Siguiente paso, solo con aprobación del dueño: preparar la entrega en `beta`, alinear `build.gradle`/`apk.json` con el asset real firmado, desplegar la Edge compartida y verificar qué revisión está activa (OPS-01). Antes de dar FIN-05 por cerrado, probar en su móvil con app cerrada dos notificaciones invertidas, reentrada, día/mes nuevos y el mismo conjunto de filas para presupuesto, disponible y saldo por banco. No promocionar a `main` sin su veredicto explícito.
