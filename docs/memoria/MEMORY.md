<!-- GENERADO POR scripts/sync-memoria.mjs — NO EDITAR A MANO.
     Espejo de la memoria del agente (MEMORY.md). Se regenera con `npm run memoria`.
     Pasado por el filtro de datos personales: el repo es PÚBLICO. -->

# Memoria — Mi Cartera

- [★★ EMPIEZA AQUÍ: traspaso del 11/9 noche](../../../../../E:/Mi cartera/docs/briefs/TRASPASO-2026-09-11-NOCHE.md) — ⚠ **`docs/briefs/TRASPASO-2026-09-11-NOCHE.md` en la rama `beta`.** Lo que él espera: (1) avisarle cuando el arreglo del saldo de su PADRE esté en producción — `tanda/saldo-padre-prod` 4.18.25, **él ya dijo que sí, solo falta el verde de Cursor**; (2) los logos de los BRÓKERS en Inversiones (sobra mi `logo:false` en los 3 sitios que agrupan por bróker); (3) **rediseñar la edición de bancos en Cartera** («muy cutrón», enseñarle diseño ANTES de implementar). Y hay trabajo SIN COMMITEAR: mirar `git status` lo primero.
- [Memoria SIEMPRE al repo](feedback-memoria-siempre-al-repo.md) — ⚠ `npm run memoria` tras escribir aquí; él trabaja también desde el móvil y con otras IAs, y el repo es PÚBLICO (el script tacha datos personales).
- [⚠ NO dar nada por hecho sin verlo](feedback-no-dar-por-hecho.md) — ⚠ 17/8, **dos veces el mismo día**: dije «test verificado» de dos guardianes que el runner **no ejecutaba**, y la tanda 3+4 se pintó «EN beta» estando solo en el disco. Enseñar el nombre del test y el código de salida REAL. **Un grep vacío es un hallazgo**, y **una tabla de estado es una afirmación** (disco → push → `version.json` → Pages; y push OK ≠ CI OK).
- [Y el plan compartido también](feedback-traspaso-a-cursor.md) — ⚠ al cerrar cada tanda, dejar en el brief del repo qué está hecho / en beta / bloqueado, para que **Cursor pueda retomar sin preguntarle**. Cursor NO es el que pica lo fácil: en bugs duros me ha superado (cerró `gestos` tras estrellarme yo).

- [★ Un rechazo suyo puede ser ANTERIOR al arreglo](rechazo-anterior-al-arreglo.md) — ⚠ 11/9: cuatro de sus cinco rechazos del 10/9 ya estaban arreglados esa misma tarde y él nunca probó la versión buena. Comparar la HORA del veredicto con la del arreglo ANTES de re-diagnosticar; le dije «no está arreglado» y era falso.
- [Integrar tandas: el merge miente](integrar-tandas-el-merge-miente.md) — ⚠ 11/9: se comió una llave de cierre, dejó DOS `var RELEASE_NOTES=` (gana el segundo) y dos notas de tandas aprobadas se quedaron fuera de Novedades. Lista de comprobaciones tras cada merge; `git status` manda, no el mensaje del merge.
- [⚠ Y el merge tambien puede fallar MUDO](integrar-el-merge-mudo.md) — ⚠ 11/9: silencie el stderr de `git merge`, la rama no existia, y todo tenia pinta de ir bien (sin conflictos, VERSION igual, suite VERDE sobre el codigo viejo). A un paso de publicar una integracion vacia como si llevara los arreglos. Nunca silenciar un merge; comprobar que HEAD avanza Y que el contenido esta.
- [★ La app se llama AELY, no Mi Cartera](aely-el-nombre-oficial.md) — ⚠ **decisión suya del 10/9, nombre OFICIAL**, con brief de marca cerrado en el repo («no inventes otra identidad»). Dentro, la línea roja: **el `applicationId` NO se toca** (sería una app nueva y su padre y su pareja pierden la suya), y qué parte viaja por beta y qué parte necesita APK.
- [★ Los logos de banco se RECORTAN, no se dibujan](logos-bancos-recortar-no-dibujar.md) — ⚠ 11/9: **cuatro** intentos a mano rechazados («parecen logos de aliexpress»). `npm run logos` recorta el isotipo del PNG oficial. Dentro, las tres reglas caras: recortes en PÍXELES (un porcentaje a ojo cortó la R de Revolut 118 px), el símbolo ocupa **0,66** del cuadro (a 0,88 = «gigante»), y Sabadell es el «BS», no la B sola.
- [★ Su ojo gana a mis medidas](feedback-el-ojo-suyo-gana-a-mis-medidas.md) — ⚠ 10/9, **dos veces en una tarde**: `rAF` daba 120 Hz clavados con el fallo puesto. Si él lo ve y mi número sale perfecto, **el instrumento es el que está mal**. Y la regla cara: **medir el gesto LENTO**, no solo el rápido — el fallo vivía entero en la velocidad que no probaba.
- [★ El 475 del widget: un signo volteado lo duplica](ob-signo-volteado-duplica.md) — ⚠ 11/9, CAUSA MEDIDA y no es el widget: el índice único lleva el SIGNO dentro, así que un movimiento de Open Banking que vuelve invertido entra **otra vez** (3 pares, ≈1.098 € de ruido, solo en su usuario). Sus 248 lápidas son él limpiándolo a mano desde julio. Dentro: `reconcileObDupes` no borra NADA pese al nombre, los 55 eventos OB son todos de TR (ciegos en Revolut, que es el que falla), y `diag-widget.mjs` etiquetaba como «widget» el ingest VIEJO — de ahí salió el falso «faltan 81,06 €».
- [★ INVENTARIO ÚNICO Y COMPLETO](mi-cartera-backlog-2026-08.md) — ⚠ **EMPEZAR POR AQUÍ. ES LA LISTA ÚNICA: lo nuevo va ahí.** Reúne las 3 fuentes que estaban sueltas (sus veredictos + backlog histórico/ChatGPT + lo nuevo), fácil→difícil. **§0-BIS = las 12 cosas del crucero (17/8)**; el orden de ataque vive en `docs/briefs/plan-vuelta-crucero.md`. Lo que más le importa: **import histórico seguro** (hoy NO lo es, 4 agujeros auditados).
- [«Al scrollear» puede ser ENTRE PESTAÑAS](feedback-scrollear-puede-ser-entre-pestanas.md) — ⚠ preguntar QUÉ gesto antes de medir; tres builds se publicaron «arregladas» midiendo el gesto que no fallaba.
- [⚠ Su móvil es un OnePlus 13, NUNCA fue un Oppo](su-movil-es-un-oneplus-13.md) — ⚠ Android 16, WebView Chromium 150. El repo lo dice mal en ~35 sitios y se descartaron hipótesis contra el sistema equivocado. Él lo sabía y callaba porque le hacía gracia. Dentro: `screenrecord` bloqueado, 120 Hz, y **nunca `adb pull /sdcard/` a pelo**.
- [Probar en su móvil EN VIVO, sin CI](entorno-en-vivo-chrome-movil.md) — ⚠ `adb reverse` + Chrome del móvil + CDP: hipótesis en segundos en vez de 11 min. Es lo que cerró el destello. Dentro: el SW sirve la copia vieja y te hace revertir arreglos buenos.
- [Saga del destello de temporada](season-destello-saga.md) — cómo aislar un efecto visual: congelar el gesto con el dedo puesto y restar dos capturas. El Δ del píxel a secas mide el contenido, no el efecto.
- [De UNO EN UNO, y verificar con sus datos reales](feedback-de-uno-en-uno.md) — ⚠ desde el 4/8 se acabaron las tandas paralelas; y nada se da por arreglado sin simularlo antes contra su estado REAL de la nube.
- [Un gasto que no entra puede ser de Google Wallet](notis-wallet-y-el-canal.md) — ⚠ mirar de qué APP salió la noti ANTES de tocar el parser; se lee de verdad con `dumpsys notification --noredact` (y sus codepoints). El canal `tapandpay` filtra mejor que el paquete.
- [★ e2e: `.first()`/`.last()` sin acotar, y CÓMO EJECUTARLOS YO](e2e-getbytext-pestanas-premontadas.md) — ⚠ **dos víctimas ya**: el premontaje de pestañas (17/8) y los logos de banco (11/9, dejaron la beta 4.19.49 SIN publicar porque su `<circle>` se coló como último de la página). Siempre dentro de su contenedor. **Y dentro: `node node_modules/@playwright/test/cli.js test --config=playwright.config.mjs` SÍ funciona** (no hay shim en `.bin`, por eso `npm run test:e2e` fallaba) — 218 e2e en 2,3 min y `npm test` con EXIT 0 real.
- [★ La misma regla en dos sitios — o en SEIS](misma-regla-en-dos-sitios.md) — ⚠ **11/9: tres veces el mismo día**, y una le pintaba a su padre 455,50 € donde el banco decía 26,46. `applyBankBalances` era la SEXTA copia sin migrar de `saldoCuentaGasto`. Antes de escribir una fórmula inversa a mano, buscar si ya existe (`valueDesdeSaldo`, `gastoDelMesPorBanco`), y sembrar **DOS bancos** en el test: con uno pasa igual de bien estando roto.
- [Saga de los duplicados de TR](tr-duplicados-saga.md) — ✅ CERRADA 4/8 («ahora siiiiiii porfin, maravillaaaaa»). Enable Banking **no manda NADA** de TR (todo null menos importe/signo/fecha). ⚠ Dos lecciones caras: una limpieza de datos **no lleva flag**, y **tocar solo el estado local no arregla nada** (la fila vuelve de la nube).
- [★ El panel de beta: SOLO lo que pueda probar hoy](panel-beta-solo-lo-probable.md) — ⚠ 11/9 noche: 56 tandas acumuladas y me las hizo limpiar a 13. Los cinco filtros (lo ya juzgado, lo invisible, lo meta, lo que depende de servidor sin desplegar, una saga = una tanda), los pasos NUMERADOS «como si fuera tontico», y que un rechazo vuelve como PASO de la tanda que lo arregla. Dentro: quitar una tanda es tandas:[], y los guardianes de esto caducan solos.
- [Leer sus veredictos ANTES de tocar código](feedback-leer-sus-veredictos-primero.md) — ⚠ `node scripts/errores.mjs --kind=beta` lo PRIMERO; el 1/8 le entregué un informe sin mirar dos tandas que ya había rechazado. Hacerle repetir un fallo es lo que más le quema.
- [Tandas: aprobada+subida se BORRA del array](feedback-tandas-desaparecen-al-subir.md) — ⚠ no se marca «hecha», se QUITA del todo (si no, su panel sigue enseñándola como pendiente). Y nunca recomendar «sube todo, incluido lo roto» cuando hay tandas mezcladas — separar a mano, o preguntar.
- [Canal beta SIEMPRE](feedback-canal-beta-siempre.md) — ⚠ nada que él note va a `main` sin pasar antes por la rama `beta` y su OK; deroga el «push directo».
- [⚠ El promote mezcla con `-X theirs`](promote-merge-theirs.md) — se traga en silencio lo que solo esté en `main` (ya se comió el CHANGELOG de 4.12.4). Tras promocionar: mirar el diff y `test:syntax`; el verde de Actions NO basta.
- [Cada promote es un cristo](promote-cada-cristo.md) — ⚠ 2026-08-06: WEBDEBUG colado, Pages caído, apk.json fantasma, tag vs rama beta. Antídoto: `npm run release:apk` + guardián WEBDEBUG + `docs/RELEASE.md`.
- [⚠ El OTA no cambia el nativo](ota-no-cambia-contrato-nativo.md) — ⚠ 17/8 noche: el bundle de `beta` dejó de mandar `afford` y la APK 41 no entiende `budgetLeft`/`safeLiq` → **la línea «Puedes gastar» DESAPARECE del widget**. Un arreglo repartido entre cliente y nativo **no puede viajar por beta**. Y `apk.json` de `beta` debe apuntar a una APK compilada DE `beta`.
- [⚠ El recorte de Novedades falla MUDO](release-notes-recorte-falla-mudo.md) — ⚠ editar `RELEASE_NOTES` a mano puede desactivar en silencio el tope de 20 versiones y colar **+72 KB** a toda la familia; ningún test lo caza, solo desaparece una línea del log del build. Tocarlo SOLO con el parser del repo.
- [⚠ «13 de 13 funciones atrasadas» es MENTIRA](servidor-al-dia-compara-fechas.md) — ⚠ 11/9: `npm run servidor` compara FECHAS DE COMMIT, no contenido. Aparqué tres tandas del panel por eso y ninguna estaba bloqueada: al desplegar, Supabase dijo «No change found». Dentro: el CLI da 403 desde su máquina (es permiso de escritura, no cuenta), la vía buena es Actions, y el workflow ya acepta UNA sola función con las migraciones apagadas.
- [Despliegue y fuente única](mi-cartera-deploy.md) — public/index.html es la fuente; CI hace stamp+minify; SW stale-while-revalidate; sin CDNs de terceros (vendor/fonts locales); push directo al acabar. ⚠ La versión canónica es el fichero `VERSION`, NO package.json. No re-stampear artefactos en un commit que no lo necesita — `docs-frescura` lo caza como código sin bump.
- [Fase 1 Supabase](fase1-supabase.md) — migración a BD+funciones+login (magic link, todo el estado a la nube).
- [Roadmap](mi-cartera-roadmap.md) — ⚠ **17/8: lo que la FAMILIA tiene ≠ lo que dice el repo.** Pages sirve **4.15.0** y `apk.json` live apunta a **4.12.0 / código 35** desde el outage de Actions del 6/8 (no es un build roto: relanzar basta). El padre corre el NATIVO de 4.12.0 → sus bugs no se pueden diagnosticar hasta que actualice. **beta = 4.16.2 / APK 40** (widget coherente), pendiente de su veredicto. Sesión 3/8 "dale caña a todo": 7 bugs+6 features pedidos de golpe, ver detalle en el fichero. Signo de TR en Open Banking sigue sin cerrar con certeza (instrumentado, pendiente su próxima sync). Rebote de pestañas = mecanismo NATIVO del navegador (cerrado). Era v4 (3.101→4.0.10) la hizo CURSOR.
- [Saga del splash nativo](splash-nativo-saga.md) — ⚠ APARCADA 2026-08-03 en `wip/splash-icono-nativo` (subida a origin, no es PR). Icono que se encoge del tamaño de Android hasta la tarjeta web: fluido y sin fogonazo negro, pero queda sin cerrar un robot verde de Android que sale SOLO al tocar el icono de verdad (no con adb) — pista: `OplusTransitionAnimationManager`/"spring slide" de ColorOS.
- [Compilar APK aquí mismo](mi-cartera-android-build.md) — Java/Gradle vía JBR de Android Studio, adb en el SDK. ⚠ NUEVO: APK `.debug` instalable EN PARALELO a la real (`applicationIdSuffix .debug`) para que pruebe cambios nativos sin release pública ni riesgo a sus datos.
- [Depurar su móvil de verdad](depurar-webview-en-su-movil.md) — adb+CDP sobre su WebView; ⚠ `gfxinfo` miente en apps WebView y grabar la traza provoca el tirón que buscas.
- [Saga TR en frío](tr-frio-saga.md) — Cap.1 ✅ CERRADO (alpha18: Android tira cookies de sesión al matar la app; snapshot/restore tr_*). **Cap.2 (2026-07-17): TR ROTA tr_refresh en cada /session → el snapshot rancio «resucitaba» un refresh consumido → 401 real.** Fix en d404d8e (restore solo jar frío + snap tras refresh/onPause/onPageFinished) — pendiente APK 28 + verificación en frío. Dentro: espía CDP reutilizable.
- [Escalado y monetización](mi-cartera-escalado.md) — estrategia por fases (Open Banking gratis vía Enable Banking, OTA Capgo, hosting Cloudflare privado, Play Store, legal); el usuario tiene miedo, todo se prueba barato y reversible.
- [Coste de tokens y Cursor](feedback-coste-tokens-cursor.md) — se queda a medias y le fastidia; localizar con Grep en vez de leer el monolito de 870 KB. Reglas de la casa en `AGENTS.md` (raíz), que Cursor lee solo. No recortar: pruebas reales, 3 idiomas, RELEASE_NOTES.
- [⚠ Árbol compartido con Cursor, y el shell se come las barras](arbol-compartido-y-barras-comidas.md) — ⚠ 11/9: los dos escribimos en `E:\Mi cartera` a la vez (se perdió una edición mía y casi publico su trabajo a medias). **Reservar ficheros por el buzón antes de tocarlos.** Y `python - <<'PY'` desde Bash **borra las `\`**: un `` perdido dejó una expresión casando de más SIN error. Para ficheros, herramienta Write.
- [NUNCA editar ficheros con PowerShell](feedback-nunca-editar-con-powershell.md) — `Get-Content|Set-Content` corrompe UTF-8 y mete BOM; costó un APK inservible publicado a su familia. Usar Edit/Write.
- [NUNCA decir "no se puede"](feedback-siempre-se-puede.md) — investigar hasta encontrar una vía; presentar opciones, no cierres.
- [RELEASE_NOTES siempre](feedback-release-notes-siempre.md) — toda versión publicada (incluidas .1) lleva su entrada en el popup de Novedades. ⚠ **TONO (2026-07-26): genéricas y en cristiano, NUNCA dirigidas a él ni técnicas** — las lee toda la familia; las de la 4.11.0 son el contraejemplo y hay que reescribirlas.
- [Modelos dinámicos](feedback-modelos-dinamicos.md) — el usuario autoriza subagentes con modelo más barato (haiku/sonnet) para subtareas mecánicas; el hilo principal no puede auto-cambiarse de modelo (él puede con /model). Push directo al acabar cada tanda (ver deploy).
- [Backlog "para más adelante"](mi-cartera-backlog.md) — ⚠ **YA VOLCADO en el inventario único de arriba (4/8); esto queda como archivo con el DETALLE largo** (diseño del Hogar, historia del nombre, qué se rechazó a propósito de la review). Incluye la **review externa de ChatGPT (2026-07-25)**: la mitad ya estaba hecha, tabla estado-real vs tarea en `docs/ROADMAP.md`. Peticiones vivas tras la 3.100.0: push de nueva versión (necesita FCM), planes de pensiones/ahorro, todas las monedas. Más: las 6 ideas de finanzas del día a día (informe mensual = favorito de su pareja) y el Hogar/cuenta familiar (alcance decidido, diseño pendiente de su OK). Cerradas en 3.100.0: oro de Revolut y el «cuadro» de amortización (era el DIÁLOGO, no la tabla).
- [★ Menos texto, más trabajo](feedback-menos-texto-mas-trabajo.md) — ⚠ 8/9: *«DEJA DE HABLAR TANTO»*. Encadenar tandas sin narrar; resumen SOLO cuando lo pida; bloqueo ≠ parada (pregunto en una línea y sigo con otra cosa).
- [⚠ Una tanda aprobada sube SOLA](feedback-tanda-desde-main.md) — ⚠ 9/9, el cabreo gordo: nace en `tanda/<id>` **desde main**, nunca commiteando al tip de beta. Si ya están mezcladas, se portan hunk a hunk; el cherry-pick con `-X theirs` compila duplicado.
- [★ TODO lo mío lo revisa Cursor](feedback-todo-lo-mio-revisado-por-cursor.md) — ⚠ 8/9: publiqué yo solo un bug que le reapareció en la cara. Nada mío va a `beta` sin su OK, y **revisar = EJECUTAR** los e2e que yo no puedo, no leer el diff (la primera review sin ejecutar dejó la CI roja y la beta SIN publicar).
- [★ DIRIJO YO el equipo (desde 7/9)](canal-equipo-tres-agentes.md) — ⚠ decisión suya: Cursor implementa lo que yo reparta, Codex solo apoyo puntual. **Dirigir aquí = gastar poco** (Codex se fundía los tokens sin cerrar tanda). Buzón en `.claude/canal-equipo`, **gitignored**: el repo no lo menciona. Mi watcher muere al cerrar la app. Dentro, el hallazgo del índice UNIQUE sin banco.
- [⚠ Dirijo, pero NO decido solo](feedback-consenso-de-las-tres-ias.md) — ⚠ 7/9: *«te equivocas muchísimo y necesito confirmación de las IAs»*. Toda decisión va a **voto de Codex y Cursor** antes de tocar. Y si **Codex calla >30 min** se ha fundido los tokens: el trabajo pasa a **Cursor**, nunca tirar solo.
- [⚠ Subir a beta SIN preguntar](feedback-publicar-en-beta-sin-preguntar.md) — ⚠ 7/9, tirón de orejas: tanda terminada y validada **se publica y se le avisa**; él aprueba o rechaza **probándola**. Pedirle permiso para publicar es hacerle de cuello de botella. **Main sigue necesitando su OK.**
- [⚠ Codex solo si estamos bloqueados + menos volumen](feedback-escalar-a-codex-solo-si-bloqueados.md) — ⚠ 7/9: escalera **Cursor+yo → Codex (solo bloqueo real) → él**; le queda **un mes** de Codex y consume una barbaridad. Y a él, contarle **solo lo que sube y qué probar**, no el proceso.
